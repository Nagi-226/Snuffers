# 交接备忘录（2026-07-30 · W7 Qoder）

> 下次开工先读本文件 + `00-spike-plan.md` §11（最新机主决策）。
> 分支 `dev_godot`，HEAD = `8e4bf0c`。

## 今日完成

- **昼夜控制器**（`godot/scenes/levels/day_night_controller.tscn` + `godot/scripts/levels/day_night_controller.gd`）：
  - 自包含 WorldEnvironment + SunLight 子节点，实例化即用
  - 白天/黑夜两套环境参数（天空色、环境光、雾效、光源颜色/强度/角度）
  - 黑夜基线对齐 greybox_arena.tscn 的 Environment_night + MoonLight
  - `@export initial_mode` 支持开局配置；`set_mode()` / `toggle_mode()` 运行时切换
  - `mode_changed` 本地信号供同域 UI 监听
  - `is_night()` 查询接口供夜视仪可用性判定

- **夜视仪占位效果**（`godot/scripts/levels/night_vision_overlay.gd`）：
  - CanvasLayer + 全屏 ColorRect + ShaderMaterial（绿色磷光 + 扫描线 + 暗角）
  - 监听已有契约信号 `Events.night_vision_toggled`（events.gd L81），零新增信号
  - 读取 `GameState.night_vision` 同步状态
  - 白天模式禁止开启（通过 `day_night_ref.is_night()` 判定）
  - 演示场景内自行处理 N 键（集成后由 player_controller 统一发信号，本段可删）

- **演示关卡**（`godot/scenes/levels/day_night_demo.tscn` + `godot/scripts/levels/day_night_demo_controller.gd`）：
  - 独立可运行：实例化 DayNightController + NightVisionOverlay + 灰盒几何 + 飞行相机 + UI 提示
  - T 键切昼夜、N 键夜视、WASD+QE 飞行、Esc 释放鼠标
  - 模式切换时 UI 标签实时刷新

- **L1 全链 PASS**：23 脚本编译 / 15 场景实例化 / 契约 44 断言 / 节点断言全绿

## 契约变更申请

**无。** 本次施工完全复用已有契约：
- `Events.night_vision_toggled(enabled: bool)` — events.gd L81
- `GameState.night_vision: bool` — game_state.gd L35

环境参数（白天/黑夜光照数值）为视觉调参，非手感参数，暂定于 `day_night_controller.gd` 本地常量。如蜂后认为需契约化（迁入 game_config.gd），申请追加以下常量段：

```
# ===== 昼夜环境（W7 视觉调参，G4 后美术替换）=====
const DAY_BG_COLOR / DAY_AMBIENT_COLOR / DAY_AMBIENT_ENERGY / DAY_FOG_*
const NIGHT_BG_COLOR / NIGHT_AMBIENT_COLOR / NIGHT_AMBIENT_ENERGY / NIGHT_FOG_*
const DAY_LIGHT_COLOR / DAY_LIGHT_ENERGY / DAY_LIGHT_ROTATION
const NIGHT_LIGHT_COLOR / NIGHT_LIGHT_ENERGY / NIGHT_LIGHT_ROTATION
```

## ⏳ 给下一棒的注意事项

1. **player_controller.gd 键位 V→N**（W1 域）：§11.2 裁决夜视仪键位为 N，当前 player_controller.gd L18 仍为 `KEY_NIGHT_VISION: Key = KEY_V`。集成时 W1 需改为 `KEY_N`。本域演示场景已按 N 键实现。
2. **greybox_arena 集成路径**：蜂后总装时，在 greybox_arena.tscn 中实例化 `day_night_controller.tscn`，删除原有 WorldEnvironment + MoonLight 节点即可。NightVisionOverlay 挂到 HUD 层或独立 CanvasLayer。
3. **NightVisionOverlay 演示段可删**：集成后 `_unhandled_input` 中的 N 键处理由 player_controller 统一负责，overlay 只保留 `_on_night_vision_toggled` 响应。
4. **美术 G4 后**：当前夜视效果为 shader 占位（绿色磷光 + 扫描线），G4 后由 W5 替换正式夜视素材/后期。昼夜环境参数届时同步替换美术资产。
5. **L1 串行**：本工作树同一时间只允许一个 Agent 跑 `check.ps1`（.godot 缓存锁）。

## 环境锚点

- 引擎：`D:\Godot_v4.6.2-stable_win64.exe\Godot_v4.6.2-stable_win64.exe`
- L1：`"C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe" -NoProfile -File godot/tools/check.ps1`
- 演示入口：Godot 编辑器打开 `godot/scenes/levels/day_night_demo.tscn` → F6 运行
- HEAD：`8e4bf0c`（3 commits: 控制器 / 夜视仪 / 演示场景）
