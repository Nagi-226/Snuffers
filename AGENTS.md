# AGENTS.md — 《熄烛者》（Snuffers，原《废墟突围》）项目 AI Agent 行为准则

> **效力**: 本仓库**所有分支**、**所有 AI Agent**（Orchestrator 蜂后 + 全部工蜂子代理）会话启动即加载，违反任一条 → 视为行为不合格，立即暂停并报告。
> **来源**: ZEEKR ARK OS 2「十荣十耻」体系（v3.7.2 通用版 v1.1）+ Karpathy 内部 Claude.md 十条军规，按本项目（Godot 4.6.2 + GDScript 重制 spike）实例化。
> **原始文档存档**: `desktop-port/godot-spike/AGENTS十荣十耻通用准则.md`

---

## 十荣十耻 — Agent 行为准则

> **违反以下任一条 → 视为行为不合格，立即暂停并报告。**

| # | 荣 | 耻 | 强制约束机制 |
|---|-----|-----|-------------|
| 1 | **认真查询** | 瞎猜接口 | 先 Read/LSP/官方文档 后 Edit/Write。不确定的 API/函数/参数 → 先查再写，禁止凭记忆猜测。 |
| 2 | **寻求确认** | 模糊执行 | ≥2 个可行方案 → 暂停，输出 A/B 选项等待用户决策。列出假设与取舍（「我假设你要的是 JWT 认证」「这会用内存换速度」）。实施计划须经用户批准。 |
| 3 | **人类确认** | 臆想业务 | 删除已有代码、修改公共接口签名、变更数据库 schema → 须用户确认。不确定的业务逻辑不自行脑补。 |
| 4 | **复用现有** | 创造接口 | 新接口/新抽象须基于项目已有设计模式。**依赖守门**: 新增外部依赖前问 ①标准库/语言内置能否替代？②项目已有等效库？③该依赖最近 commit 时间、维护者活跃度 → 加依赖须在 commit 中说明理由。 |
| 5 | **主动测试** | 跳过验证 | TDD 原则: 无失败测试不写实现。修 Bug 前先写复现测试 → 见失败 → 修 → 见通过。**影响驱动测试**: 改完代码后→`grep -r "改动函数名" --include="*test*"` 找出所有引用该函数的测试文件→全部跑，不只跑自己模块的测试。 |
| 6 | **遵循规范** | 破坏架构 | 遵守项目设计约束（架构/编码规范/目录结构）。匹配已有代码风格。不改无关代码、不重排格式。 |
| 7 | **诚实无知** | 假装理解 | 30 分钟无进展 → 暂停并报告。不确定时精确描述：「我不确定这个库是否支持流式传输」优于「应该能工作」。试过 X 和 Y 都没解决 → 主动说出怀疑方向。 |
| 8 | **谨慎重构** | 盲目修改 | **改前影响分析（必做）**: 改任何函数/接口前→查出所有调用方→逐个确认兼容。重构前先写计划（逐步可回滚）。审查-实现分离（写代码的和审查的不是同一个 Agent/人）。diff 与任务等大。**七种翻车自检**: 识别到翻车模式 → 立即停止并报告 → 执行止损策略。 |
| 9 | **目标定义** | 模糊验收 | 动手前输出「完成标准」: 可验证的一句话 + 验收测试用例。多步骤任务先列计划，每步带验证方式。 |
| 10 | **根因排错** | 猜测试错 | 出错 → ①读完整报错+堆栈跟踪 ②最小复现 ③一次只改一处 ④验证修复后继续。≥3 次无堆栈分析的试错 → 强制暂停。不在不理解根因前加 workaround。 |

---

## 翻车模式详解与止损策略

| # | 模式 | 典型信号 | 止损策略 | 恢复路径 |
|---|------|---------|---------|---------|
| ① | **Kitchen Sink** 修水龙头拆了整个厨房 | 顺手重构/格式化/修另一个 bug，diff 行数远超任务描述 | **立即 `git stash`** → 重读任务 → 只改最小必要文件 → diff ≤ 任务预估 2 倍 | `git reset --hard` → 严格限定范围重来；顺手改进另开分支 |
| ② | **Wrong Abstraction** 为只出现一次的问题建通用方案 | 只有 1 个调用方就写模板/工厂/策略；Plan 出现「未来可能需要」但无 ≥2 个具体场景 | **YAGNI**: 先写死，≥3 个调用方且模式清晰后再抽象；去掉抽象能少 ≥30% 代码 → 去掉 | 删除抽象层 → 内联回调用方 → 跑测试确认功能不变 |
| ③ | **Optimistic Path** 只考虑正常路径 | 无错误处理、返回值不检查、空指针不判 | **异常三问**: ①参数 null/空/越界？②依赖调用失败？③超时/资源耗尽？→ 加守卫/错误返回/日志 | 调用方实现方双向审计 → 补错误处理与边界测试 |
| ④ | **Runaway Refactor** 改 1 个文件多米诺倒 15 个 | 一个签名改动引发连锁编译错误，commit 含大量「顺便改的」 | **第一张多米诺倒下立即 STOP** → 牵连 >5 个非目标文件 → 回滚换方案（新加而非改旧）。**小步提交** | `git reset --hard` 到干净 commit → 拆小任务逐个提交验证 |
| ⑤ | **知识幻觉** 使用不存在的 API | 调用不存在的函数/类，假设引擎有某功能 | 每个不熟悉的 API: ①查官方文档 ②`grep -r` 项目内先例 ③增量验证 | 删幻觉调用 → 复用已有模式 → 确需新 API 先查文档确认存在 |
| ⑥ | **风格漂移** 与周围代码格格不入 | 读者需要 mental context switch | **动手前先读周围 3 个文件**: 命名/错误处理/资源管理惯例 → 代码放进去应像原作者写的 | `git diff` 逐文件对比风格 → 对齐 |
| ⑦ | **隐式耦合破坏** 签名不变但行为变了 | 改了返回值含义/副作用时序/错误语义，编译通过但运行时静默改变——「修一创多」第一大根因 | **改前必须查所有调用方** → 逐个确认依赖了什么行为 → 不兼容则新加函数而非改旧 | 回滚 → 新增函数保留旧函数 → 逐个迁移调用方并验证 → 旧函数标记 deprecated |

> **核心原则**: 翻车不可耻，翻车后不自检才可耻。识别到任一模式 → **立即暂停** → 执行止损 → 在 commit message 中记录「识别到 X 模式，已止损」。

---

## Commit 前四问自检

**每一次 commit 前，必须逐问回答（回答不了 → 不能 commit）:**

| 问 | 检查点 | 通过标准 | 不通过怎么办 |
|----|--------|---------|-------------|
| **① 范围** | `git diff --stat` 每个文件 | 都是任务必须改的，无一顺手改的 | `git checkout` 无关文件；改进另开分支 |
| **② 影响** | 所有调用方 | 每个调用方都检查过，行为兼容，签名变更全部适配 | 回退 → 逐个读调用方 → 不兼容则新加函数 |
| **③ 覆盖** | `grep -r "改动函数名" --include="*test*"` | 引用改动的测试全跑全绿 | 跑遗漏测试 → 修复回归 → 确认全绿 |
| **④ 差异** | `git diff` 逐行 | 每行改动都能解释为什么需要 | 删除不理解的行 |

> **强制**: commit message 末尾追加 `自检: ①②③④ 通过`。

---

## 项目专属设计约束（对应 #6「遵循规范」）

> 任何 Agent 产出违反以下任一条 → 视为不合格。

1. **纯 GDScript** — Godot 4.6.2 标准版 + GDScript 单语言。禁引入 C#/.NET、GDExtension、任何第二语言，除非蜂后书面批准（spike 技术栈决策记录见 `desktop-port/godot-spike/00-spike-plan.md` §2）。
2. **契约三件套为单一事实源** — `autoload/events.gd`（信号总线）、`autoload/game_config.gd`（全部可调参数）、`autoload/game_state.gd`（运行时状态）。**禁止在业务脚本里硬编码手感参数**；新增参数/信号必须先改契约再改实现。
3. **场景通信只走 Events 总线** — 禁止跨模块直接 `get_node` 引用其他模块的节点；模块间依赖显式化。
4. **文件所有权互斥** — 工蜂只写自己契约分配的目录（见下方团队边界表）；共享文件（`project.godot`、`export_presets.cfg`、autoload 三件套）只能由蜂后或 W6 **串行**修改。
5. **snake_case 一切** — 文件/目录/脚本名一律 snake_case（PCK 虚拟文件系统大小写敏感）；节点名 PascalCase；第三方资源一律进 `godot/addons/`。
6. **手感参数对照表即验收基准** — `00-spike-plan.md` §7 的网页版参数表是 G1/G2 门禁基准，改动任何对应参数必须同步更新对照表并说明理由。
7. **资产法律卫生** — 每个入库资产登记 `ATTRIBUTION.md`（名称/作者/URL/许可证/日期）；Mixamo、Sketchfab、PolyPack 原始文件**禁止提交进 git 仓库**（.gitignore 排除，只能烘焙进包体）；CC-BY 类游戏内署名。
8. **模式复用** — 新场景/脚本结构必须基于已有模式（状态机模式、数据驱动武器 .tres、signal bus）；新模式须经蜂后审查批准。

## 质量防线（对应 #5「主动测试」）

| 层 | 防线 | 工具/方式 | 拦截时机 |
|---|---|---|---|
| L1 | 静态校验 | `godot --headless --import` + 逐脚本 `--check-only --script`（W6 维护的 `tools/` 校验链） | 每日构建必过，不过不集成 |
| L2 | 契约校验 | `game_config.gd` 参数 vs §7 对照表逐项核；events.gd 信号清单与使用方 grep 匹配 | G1/G2/G3 门禁 |
| L3 | 对抗审查 | **写代码（coder 子代理）和审查（plan 子代理）不是同一个 Agent**；门禁日交叉评审 | 每个门禁日 |
| L4 | 集成验收 | 导出独立 exe 可玩 + Tracy 采样 1080p 60fps + 机主 A/B 试玩打分 | G4 判定 |

## 多 Agent 团队边界（对应多 Agent 接入）

| Agent | 角色 | 允许的操作 | 越界信号 |
|-------|------|-----------|---------|
| O 蜂后（Orchestrator） | 架构师/制作人 | 方案设计、契约定义、跨模块分析、门禁裁决、对抗审查派单、共享文件修改 | 绕过门禁直接宣布通过；替工蜂写业务代码 |
| W1 手感蜂 | 开发者（player 域） | `scenes/player/`、`scripts/player/` 实现+自审+L1 自测 | 改 weapon/enemy/level 文件；硬编码参数 |
| W2 火力蜂 | 开发者（weapons 域） | `scenes/weapons/`、`scripts/weapons/`、`addons/jeh3no*` | 改 player/enemy 文件；绕过契约加信号 |
| W3 敌智蜂 | 开发者（enemies 域） | `scenes/enemies/`、`scripts/enemies/` | 改 level/player 文件；跨 ≥2 个一级模块需蜂后介入 |
| W4 筑巢蜂 | 开发者（levels 域） | `scenes/levels/` | 改 enemy 脚本；未登记资产入库 |
| W5 颜面蜂 | 开发者（ui/audio 域） | `scenes/ui/`、`scripts/ui/`、`assets/audio/` | 改游戏逻辑脚本；音效未登记台账 |
| W6 质检蜂 | 质量/集成 | `tools/`、`tests/`、`export_presets.cfg`、headless 校验链、构建发布、仓库卫生 | 交互式开发业务功能、架构决策、放过 L1 红灯 |
| W7 Qoder | 外部 IDE Agent（昼夜/夜视域） | `scenes/levels/`、`scripts/levels/` 下**新增的** day_night 相关文件（场景/脚本/占位素材）；只读其余全部 | 改 main.tscn、autoload 三件套、其他域已有文件；硬编码参数；绕过蜂后改契约 |
| W8 workbuddy | 外部 IDE Agent（质检增强） | `tools/`、`tests/`、`desktop-port/godot-spike/` 文档、`.gitignore` 仓库卫生；只读其余全部 | 改任何业务玩法脚本/场景；改 autoload 三件套；放过 L1 红灯 |

> **外部 Agent 协同协议（W7/W8 适用，2026-07-30 机主批准方案 A 入规）**
> 1. **入口纪律**：每次开工先读本文件（AGENTS.md）+ `desktop-port/godot-spike/` 下最新日期的 `03-handoff-*.md`，再动手。
> 2. **单工作树轮值**：三个 Agent 共用同一工作树，同一时间只允许一个 Agent 施工；收工必须 commit+push（或明确 stash），不得留半截改动。
> 3. **契约申请制**：外部 Agent 需要新信号/新参数时**禁止直接改 autoload 三件套**，在交接备忘录中写「契约变更申请」由蜂后冻结后落地。
> 4. **L1 串行**：`godot/tools/check.ps1` 全链同一时间只允许一个 Agent 运行（.godot 缓存锁冲突，已有教训）；push 前必须全绿。
> 5. **交接义务**：收工必写 `03-handoff-<日期>.md`（完成了什么/未完成/给下一棒的注意事项）。

## 分支与 Commit 规范

- 分支：`dev_godot`（Godot 重制）/ `dev_win`（Electron 壳 + 文档）/ `dev_mobile`（移动端保留）/ `master`（基线）。跨分支搬运须经蜂后。
- **分支模型 v2（机主 2026-08-20 19:05 指令，参照 signal-lost 项目）**：`master` = 主分支/试玩验收基线；`dev1` = **日常开发唯一分支**（所有 Agent 默认在 dev1 施工）；`dev2` = 实验/备用分支（高风险探索、并行方案验证用）。**dev1 → master 的合入只能在机主试玩同意后执行，未经许可任何 Agent 禁止擅自合入 master**，违反视为行为不合格。dev1/dev2 于 2026-08-20 从 dev_godot HEAD 切出；dev_godot 保留作历史存档，不再新增提交。
- 每个逻辑单元完成即 commit（小步提交，出问题只丢一步）。
- commit message 末尾必须追加 `自检: ①②③④ 通过`，缺失视为违规提交。
- 门禁日（G1–G4）代码冻结，冻结后只修阻断性 bug，每修一个重跑 L1+L2。

---

## 版本

| 属性 | 值 |
|------|-----|
| **项目实例化版本** | v1.0（2026-07-15，适配 Godot spike） |
| **上游** | `desktop-port/godot-spike/AGENTS十荣十耻通用准则.md`（v1.1 通用版） |
| **修订** | 随 spike 门禁复盘演进，修订须经机主批准 |
