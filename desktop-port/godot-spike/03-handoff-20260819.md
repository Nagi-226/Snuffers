# 交接备忘录（2026-08-19 · 蜂后 Kimi Work · Phase 0a）

> 下一棒先读本文件 + `08-post-spike-roadmap.md`（后续开发路线图，含 Phase 0b 施工清单与待机主裁决项）。
> 分支 `dev_godot`，HEAD = 本文件提交后即推送（见 git log）。

## 今日完成（Phase 0a「卫生与门面」）

### 1. 代码卫生（D6 遗留三项清零其二 + 一项蜂后裁决）

- **KEY_NIGHT_VISION V→N**：核实为**已完成**（1032472 集成时已改，`player_controller.gd` 现为 `KEY_N`）——W8 备忘录 #3 过时，关闭
- **孤儿常量 KEY_RELOAD 删除**（player_controller.gd）：全仓库零引用，R 键换弹在 W2 域 `rifle_controller.gd` 自足，契约信号通路完整
- **machine_gunner.gd 过时注释翻正**：原注释称「狙击手重写 _shoot 不发本信号」与现实相反——`sniper.gd:56` 实发 `Events.enemy_fired(&"rifle")`，注释已改为现实态
- **audio_manager 退出泄漏告警 → 蜂后裁决：暂不改代码**。静态审计无泄漏路径（G1 审计 §3e 已验证 18/18、10/10 对称 disconnect；sfx/loop/ambient 播放器均为子节点随树释放；`_streams` 缓存为 RefCounted 随单例释放）。未见告警原文不盲改（十荣十耻 #10）。**Phase 0c 机主试玩时请截取告警原文**，届时按根因处置或定性为引擎退出噪音归档

### 2. 开头画面（07 §3.1 机主指令落地）

- 新增 `scenes/ui/title_screen.tscn` + `scripts/ui/title_screen.gd`：纯黑底 + EDAA 徽记居中缓显（特写 1.9× → 拉远 1.0，3.2s）+ 标题「熄烛者 SNUFFERS」+ 呼吸提示「按任意键开始作业」→ 进 main.tscn
- `project.godot` 主场景切换为 title_screen.tscn（蜂后串行修改共享文件）
- `tests/smoke_scenes.gd` 新增 title_screen 断言用例（Background/Emblem/TitleLabel/PromptLabel）
- 徽记图样基准：`godot/assets/branding/edaa_emblem.png`，已验证与 `E:\EDAA\EDAA徽记.png` **MD5 一致**（`03262476...`）

### 3. 概念图臂章修正（07 §3.3 工艺链实例）

- 核查发现：两张概念图的臂章均为 **AI 占位**（雷暴=通用红星，浓雾=圆形 glyph），均非真 EDAA 徽记
- `concept-art/fix_armbands.py`（Pillow 确定性叠加）：雷暴贴**全彩刺绣版**（黑护甲黑底自融+羽化，不盖底）；浓雾两处贴**黑白魔术贴版**（去色+对比度增强，盖黑底圆角块模拟魔术贴章）
- 产物：`*-armband-v2.jpg` ×2，**原图未改动**；第三张概念图（浓雾罩衣形态）核实为**已在 git 中（9401ace 入库）但工作树被未提交地误删**，已 `git checkout` 恢复，三张齐全

### 4. 构建验证

- L1 全链 PASS：24 脚本编译 / 16 场景实例化（含新 title_screen）/ 契约 53 断言 / 节点断言全绿
- 当日可玩包：`godot/export/snuffers_spike_20260819.exe`（121.7 MB，daily_build 产出，更名后首包）

### 5. G3/G4 现状核查（代码级取证，详见 08 路线图第一节）

**关键发现：Sprint 3 任务链主体未施工**——`mission_completed` 零发射者、`heli_timer_updated` 零发射者、`heli_unlocked` 恒 false、玩家死亡无 game_over 通路、18+2 未部署、A 方案三件套零实现。G3/G4 判定无法直接补办，Phase 0 重排为 0a（done）→ 0b（任务链施工）→ 0c（机主验收）三段式，施工清单与裁决项见 `08-post-spike-roadmap.md`。

## 待机主裁决（08 路线图第四节汇总，6 项）

最优先：**#1 A 方案三件套是否纳入 G4 前施工**（推荐是）；**#2/#3** 05 文档遗留两问（L0 皮肤时机 / EDAA 名称口径）。

## 给下一棒的注意事项

1. **Phase 0b 第一任务是 mission_director**：契约信号全复用（heli_unlock_changed/heli_called/heli_timer_updated/mission_completed/game_over 均已在 events.gd），零新增信号；GameConfig 的 HELI_* 三参数已冻结，禁止改值
2. 标题场景缓显参数（EMBLEM_REVEAL_TIME=3.2、EMBLEM_ZOOM_FROM=1.9）为脚本常量——纯演出参数非手感参数，未入契约；如蜂后复核认为需契约化再迁
3. 概念图修正脚本 `fix_armbands.py` 可复用于后续概念图（改 box 坐标即可）；正式发布级概念图还需走水印裁除工艺（07 §3.3，未做）
4. L1 串行纪律不变；本次 PowerShell 全路径从 Bash 调 check.ps1/daily_build.ps1 **成功**（此前 handoff 称会被安全策略拦截，本次加 `-ExecutionPolicy Bypass` 通过，供后续 Agent 参考）
5. EDAA 设定核对记录：06 §八「二级响应不见执火」措辞与设定 §7 原文有出入（应为「二级响应**以下**」）；浓雾捕梦网作业的设定主责是 2级提灯；「垂帘」护甲为项目侧扩展——详见 08 路线图裁决项 #4/#5

## 环境锚点

- 引擎：`D:\Godot_v4.6.2-stable_win64.exe\Godot_v4.6.2-stable_win64.exe`
- L1：`"C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe" -NoProfile -ExecutionPolicy Bypass -File godot/tools/check.ps1`
- 构建：`godot/tools/daily_build.ps1`（L1 + 导出 dated exe）
- 当日可玩包：`godot/export/snuffers_spike_20260819.exe`

## 边界声明

- 本次蜂后直接施工（机主「现在先完成 Phase 0」指令授权）：player_controller.gd（W1 域卫生项）、machine_gunner.gd（W3 域注释）、title_screen 新建（W5 域）、project.godot（共享文件，蜂后本职）、smoke_scenes.gd（W6 域断言注册）、concept-art 与文档（文档域）
- 未改任何契约文件（events/game_config/game_state 零触碰）、未改任何手感参数
