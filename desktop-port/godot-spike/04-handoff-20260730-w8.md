# 交接备忘录（2026-07-30 · W8 workbuddy 质检增强）

> 下一棒先读本文件 + `00-spike-plan.md` §11 + W7 的 `03-handoff-20260730.md`。
> 分支 `dev_godot`，HEAD = 本文件提交后即推送（见 git log）。
> 本文件与 W7 的 `03-handoff-20260730.md` 同日，W7 占 03 序号，本文件用 04 + w8 标注区分，不覆盖 W7。

## 今日完成

### 1. L1 校验链扩充（commit 32b1f3e）
- `godot/tests/smoke_contracts.gd` 参数断言由 5 项（3 项 G2 抽查）扩充至 **43 项**，
  覆盖 commit 99dc010（G2 契约冻结点）新增的全部参数，按 4 组归类：
  - 步枪后坐组 4 项（RIFLE_RECOIL_RECOVER_RATE/VIEW_ROT/Y/Z）
  - RPG 后坐+爆炸组 7 项（含 RPG_RECOIL_PITCH=0.01 G2 试玩手感裁决项）
  - 步兵行为调参组 18 项（G2 冻结自 W3 脚本常量迁入）
  - 碉堡机枪 2 项（BUNKER_PRONE_CEASEFIRE_TIME=7.0 机主签字裁决）
  - 狙击手 lethal 组 3 项（§6 死代码裁决参数化）
  - UI 反馈动效组 7 项（G2 冻结自 W5 脚本常量迁入）
- 信号断言（7 项）与 game_state 断言（3 项）不变；**全量 53 断言 headless 单跑全绿**（exit 0）
- 价值：G2 契约冻结点参数现已全量锁定，后续误改任一参数即触发断言失败（设计如此）

### 2. 构建验证（daily_build.ps1）
- `daily_build.ps1` 全流程 exit 0 = L1 全链 PASS + 导出 dated exe 成功
- L1 链：23 脚本编译 / 15 场景实例化 / 契约 53 断言（W8 扩充后）/ 节点断言全绿
- 今日可玩包：`godot/export/ruins_breakout_spike_20260730.exe`（daily_build 产出）
- 注：W7 的 day_night 文件虽 untracked，但经 L1 编译检查通过、不破坏 main.tscn 导出

### 3. 文档同步检查（夜视仪键位 V→N）
- grep `desktop-port/godot-spike/` 全域：文档层夜视仪键位**已统一为 N 键**，无残留 V 键旧文案
- 依据：`00-spike-plan.md` §11.2（裁决 N 键）、两份 handoff 均写 N 键
- **结论：无需改动**（文档已对齐）
- 残留：player_controller.gd L18 仍 `KEY_V`（业务脚本，W1 域，见问题清单 #3）

### 4. 第三方资产审查
- **主台账 `godot/ATTRIBUTION.md` 完全空**（仅表头 + 占位行），违反 AGENTS.md 约束 #7
- Jeh3no 子台账（`addons/jeh3no_fps_weapon_system/ATTRIBUTION.md`）登记完整（MIT/作者/URL/日期）
- 未在主台账登记的第三方资产清单（详见问题清单 #1）

## 问题清单（交蜂后派单）

### #1【P1 资产卫生】主台账 ATTRIBUTION.md 空，4 类资产未登记
- `godot/ATTRIBUTION.md` 仅占位行，以下资产均未登记（违反约束 #7）：
  1. jeh3no_fps_weapon_system（MIT，有完整子台账，但主台账未登记；约束 #7 要求插件也登记主表）
  2. 13 个 .mp3 音频（Mixkit Free License，`audio_manager.gd` 动态 `load()`，来源见仓库根 `audio/LICENSES.md`）
  3. 模型资产（Ammo Canister.glb / scene.gltf / Weapons.fbx，从 `.godot/editor` 缓存可见，来源待查）
  4. Ticketing.ttf 字体（来源待查）
- 建议：派 W6 质检蜂或蜂后补登主台账（W8 不擅自补登）
- 业务脚本第三方引用审计：仅 `audio_manager.gd:149/153` 的动态音频 `load()` 与 jeh3no 插件引用，其余 `preload()` 均为项目自有脚本（hit_solver.gd），无隐藏第三方依赖

### #2【P1 协同违规】W7 与 W8 同工作树并行施工
- reflog 证据（`git reflog`）：HEAD@{5}=32b1f3e(W8断言) → HEAD@{4}=46c6cbc(W7昼夜) → HEAD@{3}=7195343(W7夜视) → HEAD@{2}=8e4bf0c(W7演示) → HEAD@{1}=379627b(W7 handoff) → HEAD@{0}=8893044(W8 handoff)
- 即 W7 在 W8 commit 断言扩充后、commit handoff 前，于**同一工作树**施工了 4 个 commit
- 违反外部 Agent 协同协议 #2「单工作树轮值：同一时间只允许一个 Agent 施工」
- 勘误：先前本备忘录基于 W7 commit 前的过时快照误记"day_night 文件 untracked"，实际 `git ls-files` 确认 day_night 文件已正常 tracked 且已随 W7 推送（远程 origin/dev_godot=379627b），无残留 untracked
- 蜂后裁决：确认 W7/W8 并行是否为有意安排；若非，重申单工作树轮值纪律与 L1 串行约束

### #3【P2 代码待办，W1 域】player_controller.gd 键位 V→N
- `scripts/player/player_controller.gd` L18 仍 `KEY_NIGHT_VISION: Key = KEY_V`
- §11.2 裁决夜视仪键位 N；W7 handoff 已记录，W8 复述提醒
- 不在 W8 范围（业务脚本只读），派 W1

### #4【P2 卫生小项，D6 遗留】非 W8 范围，提醒
- player_controller.gd 孤儿常量 KEY_RELOAD（W1 删）
- machine_gunner.gd L93 过时注释（W3 改）
- audio_manager.gd 退出泄漏告警（W5 裁决）
- 来源：`03-handoff-20260721.md`「卫生小项」段

## 给下一棒的注意事项

1. **W8 已扩充断言**：G2 契约冻结点（99dc010）的 41 参数现已全量锁定在 `smoke_contracts.gd`。后续若改这些参数值，必须同步更新断言（否则 L1 红）。这是防误改设计，非缺陷。
2. **day_night 文件状态**：untracked，L1 编译通过但未进 main.tscn。下一棒集成前需先与蜂后确认 commit 策略（问题 #2）。
3. **L1 串行**：`tools/check.ps1` 全链同一时间只允许一个 Agent 跑（.godot 缓存锁）。
4. **今日可玩包**：`godot/export/ruins_breakout_spike_20260730.exe`（daily_build 产出，旧包 0721 仍在）。
5. **主台账待补登**：开工补登资产前先读 `addons/jeh3no_fps_weapon_system/ATTRIBUTION.md` 子台账。

## 环境锚点

- 引擎：`D:\Godot_v4.6.2-stable_win64.exe\Godot_v4.6.2-stable_win64.exe`
- L1 全链：`"C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe" -NoProfile -File godot/tools/check.ps1`（须全路径；从 Bash 调 PowerShell 会被安全策略拦截，须用 PowerShell 工具或终端直跑）
- 每日构建：`godot/tools/daily_build.ps1`（L1 + 导出 dated exe）
- 代理：机主已开；itch.io/Sketchfab/AssetLib 需代理，GitHub/Quaternius/Kenney 直连

## 边界声明（W8 workbuddy）

- 可写：`godot/tools/`、`godot/tests/`、`desktop-port/godot-spike/` 文档、`.gitignore`
- 只读：业务脚本/场景/autoload 三件套（本次未改任何业务文件）
- 本次实际改动文件：`godot/tests/smoke_contracts.gd`（commit 32b1f3e）+ 本 handoff 文件
